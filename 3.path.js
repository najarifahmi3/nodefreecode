const { log } = require('console')
const path=require('path') // no path address like ./ because it is a built in method
console.log(path.sep);

// you could just join path like path.join( whatever paht you want to join)
// path.absolute get you the absolute path 
